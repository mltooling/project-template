name: reuse-compliance-check

on:
  pull_request:
    branches:
    - main

jobs:
  test:
    runs-on: ubuntu-latest
    steps: 
    - uses: actions/checkout@v2
    - name: reuse-compliance-check
      uses: fsfe/reuse-action@v1.1